apiVersion: v1
kind: Secret
metadata:
  name: materiais-secret
type: Opaque
# stringData permite escrever o valor em texto puro (o Kubernetes codifica em base64).
# Valor EXCLUSIVAMENTE FICTÍCIO, apenas para demonstração.
stringData:
  APP_SECRET: "segredo-ficticio"
